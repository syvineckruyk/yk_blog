<h1>Listing posts</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Subject</th>
    <th>Body</th>
    <th>Body bg img</th>
    <th>User</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%=h post.title %></td>
    <td><%=h post.subject %></td>
    <td><%=h post.body %></td>
    <td><%=h post.body_bg_img %></td>
    <td><%=h post.user_id %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New post', new_post_path %>